---
phase: 04-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/geminiService.ts
  - src/components/TrialCountdown.tsx
  - src/types.ts
  - src/translations.ts
  - src/services/claudeService.ts
  - package.json
  - src/package.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "@google/genai does not appear in package.json or node_modules"
    - "geminiService.ts does not exist anywhere in the codebase"
    - "TrialCountdown.tsx does not exist anywhere in the codebase"
    - "No source file imports from geminiService or @google/genai"
    - "The string 'Gemini' does not appear in any .ts or .tsx source file"
    - "The webapp builds without errors after the cleanup"
  artifacts:
    - path: "src/services/claudeService.ts"
      provides: "Sole AI service — no Gemini import or comment"
    - path: "src/translations.ts"
      provides: "UI strings — analyzing string references Claude, not Gemini"
    - path: "src/types.ts"
      provides: "Type definitions — no Gemini comment block"
  key_links:
    - from: "src/App.tsx"
      to: "src/services/claudeService.ts"
      via: "processBookmarksWithClaude import (unchanged)"
      pattern: "claudeService"
---

<objective>
Remove all Gemini dead code left from the Phase 1 migration: delete the two unused files, uninstall the npm package from both package.json manifests, and scrub the handful of stale string references.

Purpose: The migration to Claude is complete. geminiService.ts and @google/genai exist only as inert cargo — they add bundle weight, create false import targets, and mislead future readers about the AI stack in use.
Output: Clean codebase with zero Gemini footprint; webapp builds and passes type-check.
</objective>

<execution_context>
@/Users/rogermasellas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/services/claudeService.ts
@src/translations.ts
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead Gemini files and uninstall the package</name>
  <files>
    src/services/geminiService.ts
    src/components/TrialCountdown.tsx
    package.json
    src/package.json
  </files>
  <action>
    1. Delete src/services/geminiService.ts — the file is fully superseded by claudeService.ts and has zero remaining importers in the codebase (confirmed: App.tsx, extension, and all services import claudeService only).

    2. Delete src/components/TrialCountdown.tsx — imports getTrialInfo from geminiService and is no longer rendered anywhere. The comment in App.tsx at line 1537 already documents it was removed ("Trial Countdown Widget removed — Claude proxy replaces Gemini"). Deleting the source file completes that intent.

    3. Uninstall @google/genai from the ROOT package.json. Run:
       npm uninstall @google/genai
       (This removes the entry from package.json and deletes the package from node_modules.)

    4. Remove @google/genai from src/package.json manually — edit the dependencies object and delete the "@google/genai" line. Then run `npm install` inside the src/ directory to sync the lockfile. (src/ is a separate npm workspace with its own package.json.)

    Do NOT touch src/App.tsx, src/services/claudeService.ts, or extension/ — those are already correct.
  </action>
  <verify>
    <automated>
      node -e "const p=require('./package.json'); if(p.dependencies['@google/genai']) process.exit(1); console.log('root package.json clean')" && \
      node -e "const p=require('./src/package.json'); if(p.dependencies && p.dependencies['@google/genai']) process.exit(1); console.log('src/package.json clean')" && \
      test ! -f src/services/geminiService.ts && echo "geminiService.ts deleted" && \
      test ! -f src/components/TrialCountdown.tsx && echo "TrialCountdown.tsx deleted" && \
      test ! -d node_modules/@google && echo "node_modules clean"
    </automated>
  </verify>
  <done>
    Both files are deleted from disk. @google/genai is absent from both package.json files and from node_modules. Running the verify commands above exits 0 with all five confirmation lines printed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scrub remaining Gemini string references and verify clean build</name>
  <files>
    src/types.ts
    src/translations.ts
    src/services/claudeService.ts
  </files>
  <action>
    Three source files still contain stale Gemini text. Apply minimal surgical edits:

    1. src/types.ts — Line 32 reads `// Gemini Service Types`. Change to `// AI Service Types` (the types below are now used by claudeService, not Gemini).

    2. src/translations.ts — Line 33 reads `analyzing: "Analitzant contingut amb Gemini..."`. Change to `analyzing: "Analitzant contingut amb Claude..."` (matches the actual AI in use).

    3. src/services/claudeService.ts — Line 10 reads `// Port from geminiService.ts — identical sanitization logic`. Change to `// Sanitization logic (migrated from legacy geminiService)` to remove the live reference to a now-deleted file, while preserving the historical note.

    After edits, verify the webapp builds without TypeScript errors:
      npm run build
    (run from the repo root or the src/ directory — wherever the build script is defined)

    Also run a final grep to confirm zero remaining Gemini occurrences in source:
      grep -r "gemini\|Gemini\|GEMINI\|@google/genai" --include="*.ts" --include="*.tsx" src/ extension/ 2>/dev/null
    The command must return no output.

    Do NOT edit App.tsx line 1537 (the HTML comment mentioning "Claude proxy replaces Gemini" is historical context and acceptable to keep — it explains why TrialCountdown was removed).
  </action>
  <verify>
    <automated>
      grep -r "gemini\|Gemini\|GEMINI\|@google/genai" --include="*.ts" --include="*.tsx" src/ extension/ 2>/dev/null && echo "FAIL: Gemini references found" || echo "PASS: no Gemini references in source"
    </automated>
  </verify>
  <done>
    The grep returns no output (exits 1, triggering the "PASS" echo). The webapp build completes without TypeScript errors. All three files contain only Claude-accurate text.
  </done>
</task>

</tasks>

<verification>
Run both checks from the repo root:

1. No Gemini in source files:
   grep -r "gemini\|Gemini\|GEMINI\|@google" --include="*.ts" --include="*.tsx" src/ extension/ 2>/dev/null
   Expected: no output

2. Package manifests clean:
   node -e "const p=require('./package.json'); console.log('@google/genai present:', !!p.dependencies['@google/genai'])"
   Expected: `@google/genai present: false`

3. Build succeeds:
   npm run build
   Expected: exits 0, no TypeScript errors
</verification>

<success_criteria>
1. `@google/genai` is absent from both package.json files and node_modules
2. `src/services/geminiService.ts` and `src/components/TrialCountdown.tsx` do not exist
3. Grep across src/ and extension/ for "gemini" or "@google" returns zero matches
4. `npm run build` exits 0 with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-tech-debt-cleanup/04-01-SUMMARY.md` following the summary template.
</output>
